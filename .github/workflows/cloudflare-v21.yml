name: Cloudflare v21 (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Architecture notice
        run: echo 'v21 Share now uses Vercel Frontend -> Supabase Database + Storage. Cloudflare deployment is retired.'
