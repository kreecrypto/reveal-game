{
  "name": "reveal-game",
  "version": "21.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check js/storage.js && node --check js/setup.js && node --check js/game.js && node --check js/share-api.js && node --check js/publish.js && node --check cloudflare-v21/src/index.js",
    "test": "playwright test",
    "test:smoke": "playwright test --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "http-server": "^14.1.1",
    "wrangler": "^4.0.0"
  }
}
