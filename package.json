{
  "name": "reveal-game",
  "version": "20.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check js/storage.js && node --check js/setup.js && node --check js/game.js",
    "test": "playwright test",
    "test:smoke": "playwright test --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "http-server": "^14.1.1"
  }
}
