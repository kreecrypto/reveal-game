{
  "name": "reveal-game-cloudflare",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply reveal-game-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply reveal-game-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
